# P3.3.18 部门 wiki publish — 实测回归 checklist

跨 4 phase + P2 polish 总 31 文件. 跑完一遍确认整套链路通.

每条独立, 失败截图给我.

---

## §0 启动

```bash
# 选项 A: 不接 PG (FS fallback, 快验)
cd ~/person_task/catfish/central/wiki-hub
pip install -e .
python -m catfish_wiki_hub.app   # port 8994

# 选项 B: 接 PG (跟生产 schema 一致)
docker run --rm -d --name catfish-pg -e POSTGRES_PASSWORD=test -p 5432:5432 postgres:16
export CATFISH_DB_URL="postgresql://postgres:test@localhost/postgres"
cd ~/person_task/catfish/central/wiki-hub
alembic upgrade head
python -m catfish_wiki_hub.app   # port 8994
```

```bash
# gateway 起来 (注册了 wiki_hub_proxy)
cd ~/person_task/catfish/central/llm-gateway
python -m catfish_gateway.app   # port 8999
```

```bash
# Companion build + 启动
cd ~/person_task/catfish/edge/companion-app
pnpm tauri build && open -a "Catfish Companion"
```

- [ ] `curl http://127.0.0.1:8994/healthz` 返 `{"status":"ok","service":"catfish-wiki-hub"}`
- [ ] Companion 启动无报错, 登录走 OIDC 后 `~/.catfish/oauth/id_token` 存在
- [ ] gateway 启动 log 含 `wiki_hub_proxy: /v1/wiki/* 反代已挂载 (P3.3.18)`

---

## §1 发布链路 (Phase 2 + Phase 3 UI)

### §1.1 第一次 share — 触发警告 + 自配敏感词

- [ ] 选某 wiki 文件 (e.g. `wiki/entities/老李.md`) 含手机号 `13800138000`
- [ ] 点 "📤 分享到部门" → dialog 弹, 顶部黄底强警告 (manifesto 公理 4)
- [ ] 头部蓝底 hint "💡 想扫客户名/项目代号? 创建模板", 点 "创建模板"
- [ ] 看到绿色 "✓ 已创建模板: /Users/.../wiki/sensitive_terms.txt", 文件真存在
- [ ] 填 `dept/finance`, 勾 "我知道已 pull 副本撤不回"
- [ ] 点 "确认分享" → 红底警告显 "扫到 1 类内容警告 — pii (1 处)"
- [ ] 点 "我看过了, 强发" → 绿底 "✓ 已 publish 到 dept/finance · file_id=xxx"
- [ ] dialog 顶部 "关闭" 关掉

### §1.2 校验 hub 真有这条

```bash
# 走 gateway (用员工 token)
TOKEN=$(cat ~/.catfish/oauth/id_token)
curl -H "Authorization: Bearer $TOKEN" http://127.0.0.1:8999/v1/wiki/documents | jq
```

- [ ] 返 `{documents: [{namespace: "dept/finance", title: "老李", ...}], count: 1}`
- [ ] body_md 含老李原文本 + `{{phone_1}}` (auto_scrub 没生效, 因为 wiki 不自动 scrub, 但 ack=true 让原文进 hub)

> 注: wiki 跟 skill 不同, 是**原文直接进 hub**, 不脱敏. 风险已经用强警告 + ack 让员工知情.

### §1.3 校验本机敏感词扫到

编辑 `~/.catfish/wiki/sensitive_terms.txt` 加一行 `FFCS`. 选一条含 "FFCS" 的 wiki, share 时:

- [ ] warnings 含 `sensitive_terms` category 命中 "FFCS"

---

## §2 拉取链路 (Phase 4 主体)

### §2.1 Dashboard WikiHubCard 列已发布

- [ ] Dashboard 切到 "技能 / Skills" section → "📚 部门 wiki" tab
- [ ] 看到 §1 刚发的 wiki, 显标题 + kind badge + description preview + publisher + 时间
- [ ] 头部 "(1 wiki / 1 部门)"
- [ ] 一行显 "安装" button (未装)

### §2.2 安装本机

- [ ] 点 "安装" → 5-15s 后绿 toast "已装 '老李' 到 ~/.catfish/wiki-shared/dept/finance/"
- [ ] 60s refresh 后该条变成绿 ✓ 已装 badge (button 消失)
- [ ] 本机 `~/.catfish/wiki-shared/dept/finance/<file_id>.md` + `<file_id>.meta.json` 真存在
- [ ] meta.json 含 `published_by` / `published_at` / `installed_at`

### §2.3 WikiTree 显部门 wiki 分组

- [ ] 切知识体系 tab → WikiTree 多出紫色 "📥 部门 wiki (read-only)" 分组
- [ ] 标题显 "老李 · finance" (含部门名)
- [ ] 点开 → WikiPreview 显内容
- [ ] action bar 显紫色 "📥 部门 wiki · read-only (来自 hub)" badge, **没**有编辑 / 删除 / 分享按钮
- [ ] action bar 显 "🗑 卸载本机副本" 按钮

---

## §3 撤回链路 (manifesto 公理 4 核心)

### §3.1 原 publisher 撤回

LLM 调 `catfish_wiki_unpublish({hub_namespace: "dept/finance", hub_file_id: "<刚才的 id>"})`. 或直接 curl:

```bash
TOKEN=$(cat ~/.catfish/oauth/id_token)
curl -X POST -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"reason":"客户翻脸了"}' \
  http://127.0.0.1:8999/v1/wiki/documents/dept%2Ffinance/<file_id>/unpublish
```

- [ ] 返 `{ok:true, summary:"已撤回...", manifesto_note:"..."}`
- [ ] PG 查 row 仍存在 (audit 保留), `stale_after_unpublish=true`, `body_md=''`, `unpublished_reason='客户翻脸了'`
- [ ] FS `~/.catfish-hub/wiki/dept/finance/<file_id>.md` 物理删

### §3.2 员工 B 视角看 stale

- [ ] Dashboard WikiHubCard 60s refresh 后:
  - 头部红 badge "⚠ 1 你装的已撤回"
  - 那条 row 红框显 "⚠ 原作者已撤回. 你本机仍有副本, 自己决定是否卸. (manifesto 公理 4 — 中央不强制清你本机)"
  - 不显安装 button (本来已装, 也不能再装)

### §3.3 WikiPreview 黄底 stale banner

- [ ] 切到知识体系 tab → 选刚才装的部门 wiki ("老李 · finance")
- [ ] 正文上方黄底 banner: "⚠ 原作者已撤回这条 wiki (2026-06-10)" + 公理 4 解释 + reason 行 "原作者撤回原因: 客户翻脸了"
- [ ] 本机文件内容仍能看 (没被清, 公理 4)

### §3.4 员工 B 自决: 卸载本机副本

- [ ] 点 "🗑 卸载本机副本" → "🗑 确认卸载" → 卸载
- [ ] WikiTree 紫色分组那条消失
- [ ] 本机文件 mv 到 `~/.catfish/wiki-shared/.trash/<ts>-wiki-shared_dept_finance_<file_id>.md` + sidecar

---

## §4 凭据扫拦截 (Phase 2 安全)

试发一条 wiki 含 `api_key: abcd1234efgh5678ijkl9012`:

- [ ] share dialog 点确认 → 红底 error "publish 前安全扫描命中疑似凭据, 拒绝上传:..." + scan_phase=credentials
- [ ] **没有** "我看过了, 强发" 选项 — 凭据永不能 ack 强发 (跟 skill 一致)

---

## §5 跨员工 happy path 演示

用 2 个员工账号 (员工 A / 员工 B) 走一遍, 验跨员工真通:

1. 员工 A: 写一条 `wiki/entities/客户A.md` → 分享到 dept/finance
2. 员工 B: Dashboard 看到 → 安装 → 知识体系 tab 看到 "客户A · finance"
3. 员工 B 修改本机部门 wiki — 应该 read-only 不让改 (action bar 没"编辑"按钮)
4. 员工 A 撤回
5. 员工 B 看 WikiHubCard 黄色提示, WikiPreview 黄底 banner. **本机文件还在**.
6. 员工 B 卸载本机副本.

- [ ] 全程跑通

---

## §6 边界

- [ ] wiki 文件超 1MB → publish 返 "wiki 文件超 1MB 上限"
- [ ] namespace 不是 dept/X 格式 → publish 返 "namespace 只允许 'dept/<部门>' 格式"
- [ ] file_id 不合法 (有 / 或 ..) → install 返 "file_id 不合法"
- [ ] hub 不可达时 → Dashboard 显 "wiki-hub 未启动" 提示

---

## 跑完报告

跑完所有 §1-§6 全 √ → 部门 wiki publish 整套 ship 完整, P3.3.18 闭环结束.

撞到任何一条不通, 截图 + 复现步骤 + 终端 log 给我修.
