# P3.3 早安 tab 改造 — 实机回归 checklist

6/10 marathon P3.3.6-11 ship 后, build + 重启 Companion 跑这份清单. 每条独立, 失败直接给我截图.

## 准备

```bash
# 清掉天气失败 cache (P3.3.8 fix2 前的死锁文件)
rm -f ~/.catfish/weather_cache.json

# build
cd ~/person_task/catfish/edge/companion-app
pnpm tauri build   # or `pnpm tauri dev` 调试
# 启动 Companion (重启已开的)
```

## §1 BriefingTab 两栏布局 (P3.3.6)

- [ ] 早安 tab 顶部有日期 + 天气 (San Jose / 你 IP 所在城市)
- [ ] 左侧 sidebar 有 `急 · N` / `中 · N` / `低 · N` 分组, 已默认处理也分组显
- [ ] 点 task 高亮变换, 右侧切到对应 task 的 detail pane
- [ ] sidebar 单独滚 (sidebar 长时滚条独立, 不影响右侧)
- [ ] detail pane 单独滚 (chat 长时滚条独立)

## §2 task chat 基础 (P3.3.7)

- [ ] 选一个 task, 右侧显 task 标题 + reason + 紧急度 badge
- [ ] 输入框输入 "今天我准备先 A 再 B, 你看顺序对吗?" → AI 流式回答
- [ ] AI 回答里 `**xxx**` 渲染粗体, `1. 2. 3.` 渲染编号列表, ``` ``` 代码块高亮
- [ ] 切走 task 再回来 — chat 历史还在
- [ ] 关 Companion 重开 — chat 历史还在 (P3.3.7 Phase 2 jsonl)
- [ ] 清空对话按钮工作, 二次确认弹

## §3 天气 (P3.3.8 + fix2)

- [ ] 顶部显 `🌤 San Jose 20°C Sunny` (或你 IP 所在城市)
- [ ] 鼠标 hover 显 tooltip `wttr.in · fetched=...`
- [ ] 如果显 "🌐 天气不可用", 点刷新按钮 → 自动重拉 (fix2 让失败 cache 视同 stale)
- [ ] 写 config: `~/.catfish/weather_config.json` 加 `{"homeCity": "北京", "tempCities": ["上海"]}` → 刷新后显两个城市并排

## §4 task_uid stable key (P3.3.9)

最关键的回归 — 之前用户撞 "task chat 一刷新就丢".

- [ ] 跟某条 task 聊 3-4 轮, 确认 chat 历史在
- [ ] 点 BriefingCard 头部 "刷新" 按钮 → advisor 重算 → task 标题可能略变
- [ ] **任 chat 历史应该还在** (uid 复用), 不应该回到空 chat
- [ ] 看终端日志, advisor 日志应该有 `[advisor] 注入上次 N 条 task_uid (复用让 chat 跨 refresh 不丢)`
- [ ] 看 `~/.catfish/task_chat/` 目录, 应该看到 6 字符 uid 命名的 jsonl 文件 (如 `csmm4z.jsonl`), 不是中文标题

老 jsonl 兼容:
- [ ] 老的 title-命名 jsonl 不应该被删 (盘上仍存 `CSMM-4 评估报告撰写与合规审查.jsonl` 等)
- [ ] 但 UI 看不到老历史是正常的 (uid 跟老 title 文件名不匹配, 兜底 fallback 只在新 uid 文件不存在时触发)

## §5 task chat tool calling + approval (P3.3.10)

测 tool 调用:
- [ ] 跟 task chat 说 "帮我起草一封回信" → AI 应该调 `catfish_draft_email_reply` tool → chat 里弹 tool 卡片 (🔧 catfish_draft_email_reply ⏳)
- [ ] tool 跑完卡片变 ✓ done, 点开看 args 和 result
- [ ] 如果 result 里有文件路径 (.docx / .md), 卡片下方有 file pill 可点

测 approval banner:
- [ ] 让 AI 调 `execute_code` (说 "跑段 python 算一下我这个月几个周末没加班") → 顶部弹 approval banner "hermes 等批准 [pattern_key] [60s 倒计时]"
- [ ] 点 "批准" → banner 消失 → tool 卡片继续跑 → result 显出来
- [ ] 点 "拒绝" → tool 卡片 status=error, AI 应该跟员工说被拒了换方案

## §6 跨重启 tool 历史还原 (P3.3.11)

- [ ] §5 完整跑完后, 关 Companion
- [ ] 重开, 回到同一条 task
- [ ] **tool 卡片应该完整还原** (含 result, 不是 "(空)")
- [ ] 看 `~/.catfish/task_chat/<uid>.jsonl`, 应该看到 tool row (role: "tool", tool_call_id: "...")

## §7 状态按钮 (回归)

- [ ] 点 "✓ 标记完成" → 头部 badge 变 "已完成"
- [ ] 点 "⏰ 推迟到明天" → badge 变 "已推迟"
- [ ] 点 "✗ 不做" → task 应该从 sidebar 消失 (ignored)
- [ ] 撤销按钮工作

## §8 已默认处理段

- [ ] sidebar 底部 "已默认处理 · N" 段显
- [ ] 显的是 `低优先归档 · 42` 这种 category · count

## 真坑预期

如果 hit 这些 — **不是 P3.3 的锅, 是更上游问题**:

| 症状 | 真因 | 找谁修 |
|---|---|---|
| advisor 一直 "刷新中" 不返 | 模型卡 / hermes 8642 死 | 看 chat.ts retry banner + hermes log |
| chat 输入框灰着不让发 | useTaskChat isStreaming 卡 true | 切走 task 再回 reset state |
| tool 卡片 ⏳ 永远不变 done | tool_bridge daemon 死 | `ps aux | grep tool-bridge` |
| approval banner 不弹但 tool 卡 | P15/P15.2 plugin 没装 / hermes 升级后失效 | 看 hermes log 找 `[P15`, `[P15.2` |

---

跑完截图 / 报问题给我. 全过 → P3.3 系列正式收工.
