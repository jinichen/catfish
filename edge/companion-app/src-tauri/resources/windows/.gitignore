# Windows msi resources (W1 BL-CATFISH-OFFLINE-INSTALL 7/11).
#
# Windows artifact 由 Windows 本地脚本或 Windows CI 在构建前生成，不进 git。
# Windows 资源现在只由 tauri.windows.conf.json 在 Windows 构建时加载，
# macOS/Linux 不再需要用空 placeholder 满足跨平台配置校验。
#
# Windows build 流程:
#   1. bash src-tauri/scripts/build-windows-resources.sh
#      → 生成真实 Windows artifact
#   2. npx tauri build --target x86_64-pc-windows-msvc --bundles msi
#      → 出 msi (~350MB)
#   3. 构建产物不提交，构建后清理 resources/windows/ 即可
#
*.log
# W2.8 (7/13) 修 encoding 根治: _phase1_win_* kit (3 files, ~20KB 总) 进 git,
# 让员工 Windows 侧走 Invoke-WebRequest 拉 GitHub raw 覆盖本地 (从此稳定).
# 只排除临时 log / 展开的 hermes-agent-src\ (从 tar 展开的~200MB tree)
_phase1_*.log
hermes-agent-src/
hermes-agent/

# 微信读取器真实 tar 由 Windows 构建阶段生成，不提交二进制产物
# 真实 tar 在 Windows 构建阶段生成；不把 Windows 空文件带入 macOS 包。

# 构建生成的 Windows 运行时资源不进 Git。
/install.ps1
/uv.exe
/cpython-3.11.15-embed.zip
/hermes-agent-bundle.tar.gz
/chromium-embed.tar.gz
/catfish-email-dist.tar.gz
/hermes-deps-dist.tar.gz
/catfish-wechat-reader-dist.tar.gz
