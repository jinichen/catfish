/** @deprecated 6/2 BL-DASHBOARD-DROP-RECORDINGS-CARD: RecordingsCard 整卡删后这层 TS
 * wrapper 没人调. 文件保留作 git blame 锚, 周一物理 rm 由鸿波拍.
 *
 * Tauri 命令 (src-tauri/src/commands/recordings.rs) 仍保留, 给 CLI / 未来
 * power-user 入口用. 如需在 TS 侧重新使用, 直接 import invoke 调 Tauri 命令.
 */
export {};
