# 2026-04-25 工作 commit 切分建议

> 这是一份临时草稿 —— commit 完可以 `rm` 掉。
>
> 建议按"逻辑域"拆 5 个 commit + 1 个 docs commit，
> 每个独立可 revert，每个 commit body 解释 what + why。

---

## Commit 1 · 身份层

```
feat(identity): SOUL.md catfish persona + install.sh symlink

新建 catfish/edge/identity/：
- SOUL.md (80 行)：定义小鲶人格 5 哲学（边缘主权 / 中央最小 /
  礼物经济 / 红线不审查 / 平权不反人）+ 语调（直接、用"你"
  不用"您"、不堆套话）+ 工具偏好（catfish-* > native）
- install.sh：备份现有 ~/.hermes/SOUL.md 到 .before-catfish，
  软链 catfish 版本

Hermes 每次对话自动 reload SOUL，无需重启即生效。这是"三层
品牌覆盖"的第一层（人格语调）。
```

**改动文件**：

```
catfish/edge/identity/SOUL.md         (新)
catfish/edge/identity/install.sh      (新)
```

---

## Commit 2 · catfish 命令包装

```
feat(branding): catfish CLI wrapper with status/doctor

写 catfish/edge/branding/catfish (195 行 bash)：
- 子命令 version / status / doctor / --raw / --no-banner
- cmd_status 一键看 gateway:8999 / Chrome:9222 / local-search /
  SOUL symlink / Hermes runtime 是否就绪
- banner ASCII 框 + 🐟 鲶 鱼 · CATFISH

这是"三层品牌覆盖"第二层 —— 员工最直接的触达点（每天敲的命令）。
```

**改动文件**：

```
catfish/edge/branding/catfish         (新, +x)
```

---

## Commit 3 · Hermes UI 深度品牌补丁

```
feat(hermes-fork): brand patch tool + 3-tier UI replacement

写可逆补丁工具：
- apply_brand_patch.py (~300 行)
  - RULES 简单字符串替换
  - REGEX_RULES 多行块替换 (HERMES_AGENT_LOGO / HERMES_CADUCEUS / TIPS)
  - 函数级替换 (扫 def build_welcome_banner，替换到下一个 def/class)
  - 备份机制 .before-catfish 后缀，--revert 一键还原
  - 已 patched 检测防重复
- uninstall.sh：调 apply_brand_patch.py --revert

落地到 ~/.hermes/hermes-agent/hermes_cli/ 三个文件：
- banner.py: HERMES_AGENT_LOGO/CADUCEUS 清空，标题改 "鲶鱼 v..."，
  build_welcome_banner 重写为 5 行极简版
- skin_engine.py: agent_name "Hermes Agent" → "鲶鱼"，
  goodbye "Goodbye! ⚕" → "再见 🐟"
- tips.py: 200+ Hermes tips → 10 条鲶鱼定制

这是"三层品牌覆盖"第三层 —— 源码级覆盖，确保任何启动 banner /
错误提示 / TUI 文案都不再出现 Hermes/Nous Research 字样。
```

**改动文件**：

```
catfish/edge/hermes-fork/apply_brand_patch.py    (新)
catfish/edge/hermes-fork/uninstall.sh            (新, +x)
```

> NOTE: `~/.hermes/hermes-agent/hermes_cli/*.py` 是 patch 应用后的
> 产物，**不**在 catfish repo 里 commit。新员工通过跑
> `apply_brand_patch.py` 重现这些改动。

---

## Commit 4 · Gateway 网络层 + Catalog 3 状态

```
feat(gateway): network abstraction + upstream self-check + catalog 3-state

新建 src/catfish_gateway/network.py (~150 行)：
- precheck_and_setup(): 启动前自检 HTTPS_PROXY 端口可达性，
  死代理主动 unset，强制 NO_PROXY 含 localhost + RFC1918 三段
- print_banner(): 一行人话状态行
- report_upstream_reachability(models): 启动后串行探每个 chat 模型
  上游可达性 (timeout 1.5s/each)，banner 输出 ✓/✗/⚠ 标记，
  返回 dict 缓存到 app.state.upstream_status

修改 catalog.py：
- 不再过滤 is_available=False 的模型 (旧行为隐藏导致前端不知道
  哪些没配 key)
- 新增 3 状态字段：
  - api_key_configured: bool
  - is_reachable: bool|None (从 app.state.upstream_status 读)
  - status_reason: str (人话解释)
前端可据此画 ✓绿（配+通）/ ⚠黄（配但不通）/ ○灰（未配）三态。

修改 app.py：
- run() 在 uvicorn 起来前调 precheck_and_setup + print_banner
- lifespan 加 report_upstream_reachability 缓存到 app.state
- /v1/catalog 端点注入 upstream_status 给 build_catalog

测试：
- tests/test_network.py 14 条单元测试，全 mock 不依赖真代理

设计原则：员工不该 care HTTPS_PROXY / Clash / VPN 这种系统级状态，
gateway 应该把这些复杂度藏在启动逻辑里。
```

**改动文件**：

```
catfish/central/llm-gateway/src/catfish_gateway/network.py   (新)
catfish/central/llm-gateway/src/catfish_gateway/app.py       (改)
catfish/central/llm-gateway/src/catfish_gateway/catalog.py   (改)
catfish/central/llm-gateway/tests/test_network.py            (新)
```

---

## Commit 5 · Companion App 初版

```
feat(companion-app): initial v0.1 — desktop sidekick for catfish

新建 catfish/edge/companion-app/ 完整组件 (69 文件)：

技术栈: Tauri 2.0 + React 18 + Vite + TypeScript + Zustand
跨平台: macOS (主) + Windows
包体: ~10MB，内存 ~50MB（vs Electron 80/200MB）

三 tab 架构（单页切换，非路由）：

控制台 tab：subprocess 真管理
- services/process.rs: 跨平台 spawn_detached (Unix process_group(0)
  / Win CREATE_NO_WINDOW) + kill (SIGTERM 800ms→SIGKILL) + is_alive
- services/catfish_paths.rs: 三级路径降级（CATFISH_ROOT → ~/person_task/
  catfish → ~/catfish）+ 找 venv python + 找 Chrome 二进制
- commands/gateway.rs: spawn 'python -m catfish_gateway.app' PORT=8999
- commands/chrome.rs: 隔离 user-data-dir 的 Chrome --remote-debugging-port=9222
- commands/local_search.rs: spawn 'python -m catfish_search.cli watch'
- 三个 status 命令: TCP + HTTP/PID 双探
- 一键全启/全停/重启 + 单服务卡片启停
- LogPanel UI 框架（实时日志流推送 TODO）

会话 tab: SQLite 真读
- 发现 ~/.hermes/sessions/ 是请求 dump，真会话在 state.db
- commands/sessions.rs (rusqlite + chrono):
  - sessions_list: SELECT 100 条最新，5 项 token 求和
  - sessions_get: 查 messages 表最后一条 user/assistant，过滤
    content NULL，4000 字符截断
- UI 列表 + 详情面板，active 徽标，endReason 标签

仪表盘 tab: 4 张卡
- 身份: 系统 $USER + SOUL symlink 判断 + skin (容忍多 key) +
  default_model + 活动会话 (state.db ended_at IS NULL)
- 配额: 占位（等中央 quota 服务）
- 模型: 消费 catalog 3 状态字段，✓绿/⚠黄/○灰 + tooltip
- Skills & MCP: 扫 ~/.hermes/skills/ 解析 SKILL.md YAML frontmatter，
  按 namespace 折叠；config.yaml mcp_servers 列表

工程基础：
- 唯一 invoke 出口 src/lib/tauri.ts（强类型包装）
- CSS variables 驱动品牌色 (tokens.css) + dark mode 自动跟随系统
- ServiceStatus 共享类型 (Rust ↔ TS) 严格对齐 camelCase

构建：
- npm install && npm run tauri:dev
- USTC 镜像 cargo / 国内 npm registry

文档：
- README + docs/ARCHITECTURE.md + docs/BRANDING.md
```

**改动文件**：

```
catfish/edge/companion-app/    (整个新目录, 69 文件)
```

---

## Commit 6 · CHANGELOG

```
docs(changelog): 2026-04-25 — 三层落地大日

身份层（SOUL + 命令包装 + Hermes UI patches）+
网络层（gateway 代理屏蔽 + 上游可达性自检）+
桌面层（Companion App 0→v0.1）一天搞定。

详细记录见 CHANGELOG.md 2026-04-25 节，含完成清单 / 踩坑（工具链
+ 设计 + 数据 + UX 四类）/ 关键认知 / 遗留 / 明天起手式。
```

**改动文件**：

```
catfish/CHANGELOG.md           (新增 2026-04-25 节)
```

---

## 整体顺序

```bash
cd /Users/chenhongbo/person_task/catfish

# 1. identity
git add edge/identity/
git commit -F- <<'EOF'
feat(identity): SOUL.md catfish persona + install.sh symlink
...（粘贴上面 Commit 1 的 body）
EOF

# 2. branding
git add edge/branding/
git commit -F- <<'EOF'
feat(branding): catfish CLI wrapper with status/doctor
...
EOF

# 3. hermes-fork
git add edge/hermes-fork/
git commit -F- <<'EOF'
feat(hermes-fork): brand patch tool + 3-tier UI replacement
...
EOF

# 4. gateway
git add central/llm-gateway/
git commit -F- <<'EOF'
feat(gateway): network abstraction + upstream self-check + catalog 3-state
...
EOF

# 5. companion-app
git add edge/companion-app/
git commit -F- <<'EOF'
feat(companion-app): initial v0.1 — desktop sidekick for catfish
...
EOF

# 6. changelog
git add CHANGELOG.md
git commit -F- <<'EOF'
docs(changelog): 2026-04-25 — 三层落地大日
...
EOF

# 一并删除这份 commit 草稿
rm COMMITS_2026-04-25.md
git add COMMITS_2026-04-25.md  # （记录删除）
git commit -m "chore: clean up commit draft"
```

---

## 备选：合成 1 个 megacommit

如果你不想拆这么细，全部一把梭也行：

```
feat: catfish 三层落地（身份 + 网络 + 桌面）一天

详见 CHANGELOG.md 2026-04-25 条目。
```

但建议拆开 —— 以后回看哪一块出问题、哪一块要 revert，
独立 commit 更好操作。
